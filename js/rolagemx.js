const ObserverOptions={threshold:0};const myObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{entry.target.classList.toggle("show",entry.isIntersecting)})},ObserverOptions);document.querySelectorAll(".container-sobre, .outerSectionDiv, .container-portfolio, .container-contato").forEach(el=>myObserver.observe(el));document.addEventListener("DOMContentLoaded",()=>{if(history.state){window.scrollTo({top:0,left:0,behavior:"instant"})}});
